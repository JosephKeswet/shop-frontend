import SignUpContent from "@/features/signup";
import React from "react";

type Props = {};

const page = (props: Props) => {
	return (
		<div>
			<SignUpContent />
		</div>
	);
};

export default page;
