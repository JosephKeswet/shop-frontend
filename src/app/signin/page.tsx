import SignInContent from "@/features/signin";
import React from "react";

type Props = {};

const page = (props: Props) => {
	return (
		<div>
			<SignInContent />
		</div>
	);
};

export default page;
